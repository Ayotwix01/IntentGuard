# Set to "true" only once you have a real MiniMax API key and have confirmed
# the endpoint/auth format in app/minimax_client.py. Leave "false" to use the
# built-in mock intent verifier (no key required).
USE_MINIMAX=false

# TODO: fill these in when you enable MiniMax (see app/minimax_client.py)
MINIMAX_API_URL=
MINIMAX_API_KEY=
